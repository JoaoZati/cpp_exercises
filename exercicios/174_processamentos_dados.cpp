// inclui bibliotecass
#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b, resultado;
    double resultado_2;

    // mesmo processo de calculo que o c
    a = 5;
    b = 2;

    resultado = a / b;
    resultado_2 = (double) a / b;

    // volta inteiro
    cout << "Resultado Inteiro: " << resultado << endl;
    cout << "Resultado Double: " << resultado_2 << endl;

    return 0;
};
